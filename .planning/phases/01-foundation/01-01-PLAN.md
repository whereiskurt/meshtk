---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/config/config.go
  - pkg/config/meshtk.yaml
  - pkg/config/config_test.go
  - internal/app/server/inspect.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - AUTH-02
  - AUTH-05

must_haves:
  truths:
    - "Config struct exposes CredCache nested struct with TTLSecs, MaxSizeMB, TableName, TableRegion, DynamoDBEndpoint, Passthrough, TimeoutSecs fields"
    - "Config struct exposes ProxyUsername and ProxyPassword fields replacing hardcoded values"
    - "Config struct exposes AdminListenAddress field"
    - "Embedded YAML provides sensible defaults for all new fields"
    - "Env var override works for nested CredCache fields via MESHTK_ prefix"
    - "generateMQTTPassword function and USER_CREATION_SEED dependency are removed"
  artifacts:
    - path: "pkg/config/config.go"
      provides: "CredCacheConfig struct, extended Server struct"
      contains: "CredCacheConfig"
    - path: "pkg/config/meshtk.yaml"
      provides: "Default values for CredCache, ProxyUsername, ProxyPassword, AdminListenAddress"
      contains: "CredCache"
    - path: "pkg/config/config_test.go"
      provides: "Unit tests for config defaults and env var override"
      contains: "TestCredCacheConfig"
  key_links:
    - from: "pkg/config/config.go"
      to: "pkg/config/meshtk.yaml"
      via: "go:embed and Viper merge"
      pattern: "DefaultConfig"
---

<objective>
Extend the existing config schema with all Phase 1 configuration fields and remove the legacy password generation function.

Purpose: All credential cache settings, proxy credentials, admin address, and passthrough allowlist must be configurable via YAML/env vars before the cache and store implementations can consume them. Removing generateMQTTPassword is a clean break from seed-based auth.

Output: Extended Server struct with CredCacheConfig, updated embedded YAML defaults, config unit tests, removed legacy auth code.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@pkg/config/config.go
@pkg/config/meshtk.yaml
@internal/app/server/inspect.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CredCacheConfig struct and extend Server struct with new fields</name>
  <files>pkg/config/config.go, pkg/config/meshtk.yaml, pkg/config/config_test.go</files>
  <behavior>
    - Test: NewConfig() returns Config where Server.CredCache.TTLSecs == 900 (default)
    - Test: NewConfig() returns Config where Server.CredCache.MaxSizeMB == 64 (default)
    - Test: NewConfig() returns Config where Server.CredCache.TableName == "run-human-electro" (default)
    - Test: NewConfig() returns Config where Server.CredCache.TableRegion == "us-east-1" (default)
    - Test: NewConfig() returns Config where Server.CredCache.DynamoDBEndpoint == "" (default)
    - Test: NewConfig() returns Config where Server.CredCache.TimeoutSecs == 5 (default)
    - Test: NewConfig() returns Config where Server.CredCache.Passthrough contains ["ghosts", "kph", "ax", "meshmap"]
    - Test: NewConfig() returns Config where Server.ProxyUsername == "public" (default)
    - Test: NewConfig() returns Config where Server.ProxyPassword == "31337" (default)
    - Test: NewConfig() returns Config where Server.AdminListenAddress == "localhost:9090" (default)
  </behavior>
  <action>
1. In `pkg/config/config.go`, add a new `CredCacheConfig` struct above the `Server` struct:
```go
type CredCacheConfig struct {
    TTLSecs          int      `default:"900"`
    MaxSizeMB        int      `default:"64"`
    TableName        string   `default:"run-human-electro"`
    TableRegion      string   `default:"us-east-1"`
    DynamoDBEndpoint string   `default:""`
    Passthrough      []string
    TimeoutSecs      int      `default:"5"`
}
```
Note: Passthrough defaults come from embedded YAML (not struct tag — struct tags for slices are unreliable with Viper).

2. Add three new fields to the `Server` struct:
```go
CredCache          CredCacheConfig `json:"CredCache"`
ProxyUsername      string          `default:"public"`
ProxyPassword      string          `default:"31337"`
AdminListenAddress string          `default:"localhost:9090"`
```

3. In `pkg/config/meshtk.yaml`, add under the `Server:` section (after existing fields):
```yaml
  ProxyUsername: "public"
  ProxyPassword: "31337"
  AdminListenAddress: "localhost:9090"
  CredCache:
    TTLSecs: 900
    MaxSizeMB: 64
    TableName: "run-human-electro"
    TableRegion: "us-east-1"
    DynamoDBEndpoint: ""
    TimeoutSecs: 5
    Passthrough:
      - "ghosts"
      - "kph"
      - "ax"
      - "meshmap"
```

4. Create `pkg/config/config_test.go` with tests verifying all defaults load correctly via NewConfig(). The test function `TestCredCacheConfigDefaults` should call NewConfig() and assert each field value matches expected defaults. For the Passthrough slice, verify length is 4 and contains the expected usernames.

Also add `TestProxyCredsDefaults` verifying ProxyUsername and ProxyPassword defaults.

Also add `TestAdminListenAddressDefault` verifying AdminListenAddress default.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go test ./pkg/config/ -run "TestCredCacheConfig|TestProxyCreds|TestAdminListenAddress" -v -count=1</automated>
  </verify>
  <done>CredCacheConfig struct exists with all fields, Server struct extended with CredCache/ProxyUsername/ProxyPassword/AdminListenAddress, embedded YAML updated with defaults, all config tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Remove generateMQTTPassword and USER_CREATION_SEED dependency</name>
  <files>internal/app/server/inspect.go</files>
  <action>
1. In `internal/app/server/inspect.go`, remove the `generateMQTTPassword` function (lines 65-70).

2. Remove the `"os"` import if it becomes unused after removing `os.Getenv("USER_CREATION_SEED")` reference at line 106.

3. Remove unused imports that were only needed by generateMQTTPassword: `"crypto/sha256"`, `"encoding/hex"` (check if used elsewhere in the file first — they are NOT used elsewhere).

4. In the `inspectRawPacket` method, the ConnectPacket case (lines 99-115) currently calls generateMQTTPassword. Replace the entire auth block (lines 99-115) with a temporary stub that preserves passthrough behavior but removes the seed-based auth. The stub should:
   - Keep the passthrough check for "ghosts", "kph", "ax", "meshmap" (these will move to config in Phase 2 when wired)
   - For all other usernames, keep the existing clobber behavior (set empty username/password) as a safe default — Phase 2 will replace this with cache-backed auth
   - Remove the generateMQTTPassword call and the else branch that used it
   - Add a comment: `// TODO(phase2): Replace with credential cache lookup`

5. Verify the file still compiles: `go build ./internal/app/server/`

Important: Do NOT wire CredCacheConfig into inspect.go yet — that is Phase 2 work. This task only removes the legacy code.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go build ./... 2>&1</automated>
  </verify>
  <done>generateMQTTPassword function removed, USER_CREATION_SEED dependency removed, unused crypto imports removed, passthrough check preserved as stub, project builds cleanly with no compile errors</done>
</task>

</tasks>

<verification>
1. `go test ./pkg/config/ -v -count=1` — all config tests pass
2. `go build ./...` — entire project compiles without errors
3. No references to generateMQTTPassword or USER_CREATION_SEED remain in codebase
</verification>

<success_criteria>
- All CONF-01..04 config fields exist with correct defaults verified by tests
- AUTH-02 config fields (ProxyUsername/ProxyPassword) exist with defaults verified by tests
- AUTH-05 config field (Passthrough) exists with defaults verified by tests
- Legacy generateMQTTPassword removed, project builds cleanly
- No existing proxy behavior changes beyond removing the seed-based auth check
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
