---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/credcache/types.go
  - internal/credcache/cache.go
  - internal/credcache/cache_test.go
  - internal/credcache/store.go
  - internal/credcache/store_test.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - CRED-02
  - CRED-03

must_haves:
  truths:
    - "Credential struct holds username, password, and usertype from DynamoDB"
    - "CredentialStore interface defines Fetch(ctx, username) signature"
    - "Cache stores a credential and returns it on lookup by username"
    - "Cache returns miss for unknown usernames"
    - "Cached entries expire after configured TTL"
    - "Cache reports accurate hit and miss counters via Stats()"
    - "DynamoDB adapter scans table with FilterExpression on mqttUsername"
    - "DynamoDB adapter returns ErrNotFound for unknown usernames"
    - "DynamoDB adapter uses configurable endpoint for local dev"
  artifacts:
    - path: "internal/credcache/types.go"
      provides: "Credential struct, CredentialStore interface, ErrNotFound sentinel"
      exports: ["Credential", "CredentialStore", "ErrNotFound"]
    - path: "internal/credcache/cache.go"
      provides: "Otter v2 cache wrapper with TTL, stats, Get/Set/Delete/Stats/Close"
      exports: ["Cache", "NewCache", "CacheStats"]
    - path: "internal/credcache/store.go"
      provides: "DynamoDBStore implementing CredentialStore"
      exports: ["DynamoDBStore", "NewDynamoDBStore"]
    - path: "internal/credcache/cache_test.go"
      provides: "Unit tests for cache hit/miss/TTL/stats"
      contains: "TestCache"
    - path: "internal/credcache/store_test.go"
      provides: "Unit tests for DynamoDB adapter with mock client"
      contains: "TestStore"
  key_links:
    - from: "internal/credcache/store.go"
      to: "internal/credcache/types.go"
      via: "DynamoDBStore implements CredentialStore interface"
      pattern: "func.*DynamoDBStore.*Fetch"
    - from: "internal/credcache/cache.go"
      to: "internal/credcache/types.go"
      via: "Cache stores *Credential values"
      pattern: "otter.*Cache.*string.*Credential"
---

<objective>
Create the `internal/credcache/` package with Credential types, Otter v2 cache wrapper, and DynamoDB store adapter.

Purpose: This is the core credential cache infrastructure that Phase 2 will wire into the proxy CONNECT path. The cache provides fast in-memory lookups with TTL expiry, and the store provides the DynamoDB backend for cache misses.

Output: Complete credcache package with types, cache, store, and comprehensive unit tests.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create types.go and cache.go with Otter v2 wrapper and unit tests</name>
  <files>internal/credcache/types.go, internal/credcache/cache.go, internal/credcache/cache_test.go, go.mod, go.sum</files>
  <behavior>
    - Test: NewCache(900, 64) succeeds without error
    - Test: Cache.Get("unknown") returns nil, false (miss)
    - Test: Cache.Set("alice", cred) then Cache.Get("alice") returns the credential and true (hit)
    - Test: After Set, Stats() shows hits > 0 on subsequent Gets
    - Test: Cache.Get("unknown") increments miss counter in Stats()
    - Test: Cache.Delete("alice") removes the entry, subsequent Get returns false
    - Test: Cache entry expires after TTL (use 1-second TTL, sleep 2s, verify Get returns false)
    - Test: Cache.Close() does not panic
  </behavior>
  <action>
1. Install Otter v2 dependency:
```bash
cd /Users/khundeck/working/meshtk && go get github.com/maypok86/otter/v2@v2.3.0
```

2. Create `internal/credcache/types.go`:
```go
package credcache

import (
    "context"
    "errors"
)

var ErrNotFound = errors.New("credential not found")

type Credential struct {
    Username string `dynamodbav:"mqttUsername"`
    Password string `dynamodbav:"mqttPassword"`
    Usertype string `dynamodbav:"mqttUsertype"`
}

type CredentialStore interface {
    Fetch(ctx context.Context, username string) (*Credential, error)
}
```

3. Create `internal/credcache/cache.go` wrapping Otter v2:
- `type Cache struct` with inner otter.Cache[string, *Credential] and stats counter
- `func NewCache(ttlSecs int, maxSizeMB int) (*Cache, error)` — creates Otter cache with:
  - MaximumSize set to `maxSizeMB * 10000` (approximate: ~100 bytes per credential, so 1MB = ~10K entries)
  - ExpiryCalculator using `otter.ExpiryWriting[string, *Credential](time.Duration(ttlSecs) * time.Second)`
  - StatsRecorder using `stats.NewCounter()`
- `func (c *Cache) Get(username string) (*Credential, bool)` — wraps GetIfPresent
- `func (c *Cache) Set(username string, cred *Credential)` — wraps Set
- `func (c *Cache) Delete(username string)` — wraps Delete
- `type CacheStats struct { Hits, Misses, Evictions int64; HitRate float64 }` — exported stats struct
- `func (c *Cache) Stats() CacheStats` — reads from stats counter, computes hit rate
- `func (c *Cache) Close()` — wraps Close

4. Create `internal/credcache/cache_test.go` with the tests described in the behavior block. Use Go stdlib `testing` package. For the TTL expiry test, use a 1-second TTL and `time.Sleep(2 * time.Second)` — acceptable for a unit test.

Important notes:
- Check the actual Otter v2 API docs for exact method names. The research shows `GetIfPresent`, `Set`, and `Delete` methods on the cache. Verify these against the imported package.
- The stats package is `github.com/maypok86/otter/v2/stats`. The `NewCounter()` returns a `stats.Counter` which implements `stats.Recorder`. Access stats via the counter's `Snapshot()` method which returns a `stats.Stats` struct with `Hits()`, `Misses()`, `Evictions()` methods.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go test ./internal/credcache/ -run TestCache -v -count=1</automated>
  </verify>
  <done>types.go defines Credential, CredentialStore, ErrNotFound. cache.go wraps Otter v2 with Get/Set/Delete/Stats/Close. All cache tests pass including TTL expiry test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DynamoDB store adapter with mock-based unit tests</name>
  <files>internal/credcache/store.go, internal/credcache/store_test.go, go.mod, go.sum</files>
  <behavior>
    - Test: Fetch("knownuser") with mock returning matching item returns *Credential with correct fields
    - Test: Fetch("unknownuser") with mock returning empty items returns ErrNotFound
    - Test: Fetch with mock returning error propagates the error
    - Test: DynamoDB scan uses FilterExpression on mqttUsername attribute
    - Test: DynamoDB scan uses ProjectionExpression for mqttUsername, mqttPassword, mqttUsertype
  </behavior>
  <action>
1. Install AWS SDK v2 dependencies:
```bash
cd /Users/khundeck/working/meshtk
go get github.com/aws/aws-sdk-go-v2/config
go get github.com/aws/aws-sdk-go-v2/service/dynamodb
go get github.com/aws/aws-sdk-go-v2/feature/dynamodb/expression
go get github.com/aws/aws-sdk-go-v2/feature/dynamodb/attributevalue
go get github.com/aws/aws-sdk-go-v2/aws
```

2. Create `internal/credcache/store.go`:
- Define a `DynamoDBAPI` interface with just the `Scan` method signature matching the DynamoDB client — this enables mock testing without a real AWS connection:
```go
type DynamoDBAPI interface {
    Scan(ctx context.Context, params *dynamodb.ScanInput, optFns ...func(*dynamodb.Options)) (*dynamodb.ScanOutput, error)
}
```
- `type DynamoDBStore struct` with fields: client DynamoDBAPI, tableName string
- `func NewDynamoDBStore(tableName, region, endpoint string) (*DynamoDBStore, error)` — creates AWS config with `config.LoadDefaultConfig`, overrides region, optionally sets BaseEndpoint if endpoint is non-empty, creates DynamoDB client
- `func NewDynamoDBStoreWithClient(client DynamoDBAPI, tableName string) *DynamoDBStore` — constructor for testing with mock client
- `func (s *DynamoDBStore) Fetch(ctx context.Context, username string) (*Credential, error)`:
  - Build filter expression: `expression.Name("mqttUsername").Equal(expression.Value(username))`
  - Build projection: `expression.NamesList(expression.Name("mqttUsername"), expression.Name("mqttPassword"), expression.Name("mqttUsertype"))`
  - Execute Scan with the expression
  - Handle pagination: loop on `LastEvaluatedKey` (if result has more pages, continue scanning)
  - If no items found across all pages, return `nil, ErrNotFound`
  - Unmarshal first matching item into `*Credential` using `attributevalue.UnmarshalMap`
  - Return the credential

3. Create `internal/credcache/store_test.go`:
- Define a `mockDynamoDBClient` struct implementing `DynamoDBAPI` interface
- The mock's `Scan` method returns pre-configured `*dynamodb.ScanOutput` or error based on test setup
- Write tests for: successful fetch, not found, error propagation
- Verify the ScanInput received by mock contains the expected FilterExpression and ProjectionExpression (capture and inspect the input in the mock)

Do NOT connect to real DynamoDB in unit tests. All tests use the mock client.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go test ./internal/credcache/ -run TestStore -v -count=1</automated>
  </verify>
  <done>store.go implements DynamoDBStore with Fetch using Scan+FilterExpression+Projection+pagination. All store tests pass using mock DynamoDB client. No real AWS connection needed for tests.</done>
</task>

</tasks>

<verification>
1. `go test ./internal/credcache/ -v -count=1` — all cache and store tests pass
2. `go build ./...` — entire project compiles without errors
3. `go vet ./internal/credcache/` — no vet warnings
</verification>

<success_criteria>
- CRED-02: Cache stores/returns credentials with configurable max size — verified by cache unit tests
- CRED-03: Cache entries expire after configurable TTL — verified by TTL expiry test
- CredentialStore interface defined for DynamoDB backend swappability
- DynamoDB adapter scans with correct filter and projection expressions — verified by mock tests
- All tests pass, project builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
